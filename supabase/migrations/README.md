# Migrations

Migration `0006_whatsapp.sql` has not been applied to any Supabase project (local or remote) as of this branch. Apply manually via `npx supabase db push` (with the correct project ref/local stack configured) before deploying the WhatsApp module — until then, `/whatsapp` will error on the missing tables.
