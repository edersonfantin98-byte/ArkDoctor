# Committed on purpose: these values are public (shipped to the browser).
# Secrets (service-role key, Turnstile secret) stay in the Cloudflare project settings.

# Cloudflare Turnstile site key for the public booking form (/agendar).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAEiKpE4uZ-ZFrsOD
